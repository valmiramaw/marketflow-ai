import type { Metadata } from 'next'

export const metadata: Metadata = { title: 'Marke & Branding' }

export default function Layout({ children }: { children: React.ReactNode }) {
  return children
}
